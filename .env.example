DATABASE_URL="postgresql://practicum:CHANGE_ME@localhost:5432/practicum_vault?schema=public"
POSTGRES_PASSWORD=
SESSION_SECRET=
# AES-256-GCM: exactly 64 hex characters (32 bytes). Generate with:
#   openssl rand -hex 32
ENCRYPTION_KEY=
# Required for first-run setup in production. Generate with: openssl rand -hex 32
SETUP_BOOTSTRAP_TOKEN=
# Set to "true" only when the app is behind a trusted reverse proxy
TRUST_PROXY=false
